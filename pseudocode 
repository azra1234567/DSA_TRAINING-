'''#include <vector>
#include <algorithm>
int maxsubarraymaxBruteforce(vector<int>&nums){
    int n =nums.size();
    int maxsum=int_MIN;
    for(int i=0;i<n;i++){
        int sum=0;
        for(int j=i;j<n;j++){
            sum+=nums[j];
            maxsum=max(maxsum,sum);
        }
    }
    return maxsum;
} 
'''

array= [1:5,4:5,4:2,3:4,5:5,6:6,7:7,8:8,9:9,10:10]
frequency={}
for num in array:
    if num in frequency:
        frequency[num]+=1
    else:
        frequency[num]=1
max_freq=0
for key
